<div>
  <h4>Comment by <%= comment.author_name %></h4>
  <span> <i> Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later </i></span>
  <p class="comment"><%= comment.body %></p>
</div>